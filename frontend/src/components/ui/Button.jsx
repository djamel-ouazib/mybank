/**
 * Button component
 * Renders a simple HTML button element.
 *
 * @param {string} text - The label displayed inside the button
 */
export default function Button({ text }) {
    return <button>{text}</button>
}
